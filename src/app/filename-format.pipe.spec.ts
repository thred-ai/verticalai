import { FilenameFormatPipe } from './filename-format.pipe';

describe('FilenameFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
