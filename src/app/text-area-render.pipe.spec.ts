import { TextAreaRenderPipe } from './text-area-render.pipe';

describe('TextAreaRenderPipe', () => {
  it('create an instance', () => {
    const pipe = new TextAreaRenderPipe();
    expect(pipe).toBeTruthy();
  });
});
